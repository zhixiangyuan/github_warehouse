dir=$(pwd)
filename=自己动手写Java虚拟机.pdf
echo "${filename}_1 >> 自己动手写Java虚拟机.pdf"
cat ${dir}/${filename}_1 >> ${dir}/${filename}
echo "${filename}_2 >> 自己动手写Java虚拟机.pdf"
cat ${dir}/${filename}_2 >> ${dir}/${filename}
echo "${filename}_3 >> 自己动手写Java虚拟机.pdf"
cat ${dir}/${filename}_3 >> ${dir}/${filename}
